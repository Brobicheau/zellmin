[package]
name = "zitree"
version = "0.1.0"
edition = "2021"

[dependencies]
zellij-tile = "=0.44.1"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[profile.release]
lto = true
strip = true
